# gb-532 侧扫声呐测线覆盖缺口规划

请生成一个名为 `sonar-survey-coverage-planner` 的 Go 全栈 Web 项目，面向海洋测绘团队，用于规划侧扫声呐测线、导入航迹扫幅、识别漏测区域并提出补测线建议。本系统只做离线测绘决策支持与仿真，不连接或控制真实船舶、声呐和自动驾驶设备；不得实现商城、电商、预约、订单、仓储库存、财务、客服工单、博客或大众社区功能。

## 1. 项目概述与复杂度下限

- 项目类型：前后端分离的 B2B 全栈 Web 应用。
- 必须实现 4 个核心实体、5 个核心页面、至少 3 个共享组件、至少 2 个 hooks、至少 4 个后端中间件和 2 组前后端一致的共享枚举。
- 4 个实体必须贯穿 PostgreSQL、Go 分层和 React type/api/store/page；页面交互必须调用真实 API。
- 本项目用于验证跨文件协同改动能力。严禁合并职责到单一文件，后续字段、状态和算法版本变化应触达至少 5 个文件。

## 2. 核心实体

1. `SurveyArea`（测区）
   - 字段：`id`、`area_code`、`name`、`boundary_geojson`、`target_resolution_m`、`coordinate_system`、`default_swath_m`、`owner_team`、`status`、`created_at`。
   - 关系与动作：包含多套测线规划和多次声呐运行；支持创建、边界校验、版本更新和归档。
   - 必须贯穿 `survey_areas` 表 -> 独立 `model/dto/repository/service/handler/router` 文件 -> `frontend/src/types/survey-area.ts`、`api/survey-area.ts`、`stores/survey-area.ts` 和测区页面。
2. `TransectPlan`（测线规划）
   - 字段：`id`、`survey_area_id`、`name`、`line_geojson`、`planned_heading`、`planned_swath_m`、`line_spacing_m`、`plan_state`、`version`、`created_by`、`updated_at`。
   - 关系与动作：隶属测区并关联多次运行；支持生成平行测线、手动调整、锁定和复制新版本。
   - 必须贯穿 `transect_plans` 表及对应独立后端分层、前端 type/api/store/page 文件。
3. `SonarRun`（声呐运行）
   - 字段：`id`、`transect_plan_id`、`run_code`、`track_geojson`、`actual_swath_m`、`started_at`、`ended_at`、`navigation_quality`、`run_state`、`source_checksum`、`imported_by`。
   - 关系与动作：隶属测线规划并产生覆盖几何；支持导入、质量校验、处理、重处理和作废。
   - 必须贯穿 `sonar_runs` 表及对应独立后端分层、前端 type/api/store/page 文件。
4. `CoverageGap`（覆盖缺口）
   - 字段：`id`、`survey_area_id`、`source_run_ids`、`gap_geojson`、`area_square_m`、`gap_ratio`、`severity`、`recommended_line_geojson`、`algorithm_version`、`gap_state`、`explanation`、`detected_at`。
   - 关系与动作：由测区和运行冻结快照产生；支持检测、确认误报、接受补测建议和关闭，历史结果不可覆盖。
   - 必须贯穿 `coverage_gaps` 表及对应独立后端分层、前端 type/api/store/page 文件。

## 3. 差异化算法与状态流

- 根据每条实际航迹和 `actual_swath_m` 生成扫幅多边形，对测区边界求交并计算覆盖并集、重复覆盖、边缘裁剪和内部漏测多边形。
- 对小于分辨率阈值的碎片做可解释过滤；按缺口主方向和相邻测线间距生成补测线建议，输出覆盖率、重叠率、漏测率及输入版本哈希。
- `SonarRun` 状态流：`imported -> quality_checked -> processing -> processed`，任一步可到 `rejected`，`processed -> superseded`；非法迁移返回 409。
- `CoverageGap` 状态流：`detected -> reviewed -> accepted/false_positive -> resurveyed -> closed`。同一输入哈希与算法版本必须幂等。
- 坐标计算必须明确采用项目约定的平面坐标系；禁止用经纬度直接做米制面积。算法仅辅助测绘人员，不得下发船舶控制指令。

## 4. 核心页面与实体消费

1. `/areas` 测区管理：消费 `SurveyArea + TransectPlan`，显示边界、规划数量和覆盖摘要。
2. `/plans` 测线规划器：消费 `TransectPlan + SurveyArea`，编辑测线、间距和版本，前端地图使用二维 Canvas/地图组件，不依赖外部在线服务。
3. `/runs` 航迹处理台：消费 `SonarRun + TransectPlan`，导入示例 GeoJSON、校验质量并发起处理。
4. `/coverage` 覆盖分析：消费 `CoverageGap + SonarRun + SurveyArea`，叠加显示扫幅、重叠区、缺口和补测线。
5. `/audit` 审计中心：消费四实体变更投影与审计记录，支持 request ID、实体和操作者筛选。

`MapLegend` 被测区、规划和覆盖页共用；`RunStateBadge` 被运行和覆盖页共用；`GeometryDetailDrawer` 被规划、覆盖和审计页共用。

## 5. 横切关注点

- JWT + RBAC：角色至少为 `admin`、`survey_planner`、`data_processor`、`reviewer`、`auditor`；联动数据库角色、JWT claims、Go auth/rbac 中间件、路由、React 路由守卫、按钮显隐和类型。审计员只读，只有 reviewer 可确认缺口。
- 操作与算法审计：所有变更记录前后快照、操作者、request ID；算法记录输入 checksum、坐标系、版本、参数、耗时与结果摘要。
- 全局错误：统一业务错误码和 HTTP 状态；无效 GeoJSON/坐标系返回 422，版本冲突返回 409，前端统一处理。
- 限流与幂等：导入和覆盖计算接口限流；运行计算使用 `Idempotency-Key` 和输入哈希防重复。

## 6. 共享枚举、组件、hooks 与中间件

- `RunState = imported | quality_checked | processing | processed | rejected | superseded`：后端 `backend/internal/constants/run_state.go`，前端 `frontend/src/types/enums/run-state.ts`。
- `GapSeverity = minor | major | critical`：后端 `backend/internal/constants/gap_severity.go`，前端 `frontend/src/types/enums/gap-severity.ts`。
- README 必须列出两组枚举在 model、dto、service 状态机、store、组件、页面和测试中的所有位置。
- 共享组件：`MapLegend`、`RunStateBadge`、`GeometryDetailDrawer`；hooks：`useAuth`、`useCoverageLayers`。
- 后端至少独立实现 `request_id.go`、`recovery.go`、`auth.go`、`rbac.go`、`audit.go`、`error_handler.go`。

## 7. 技术、规模与文件结构

- 英文短名：`sonar-survey-coverage-planner`。
- 前端：React 18 + TypeScript + Vite + Material UI + Zustand；后端：Go 1.22 + Gin + GORM；正式数据库：PostgreSQL 16 + PostGIS 扩展，runtime smoke 支持 SQLite；认证：JWT。几何运算必须使用成熟 Go/PostGIS 能力并提供固定夹具测试，不手写脆弱字符串解析器。
- API 统一使用 `/api/v1`，另提供真实 `/healthz`；使用 validator/v10、统一响应和错误码、`%w` 错误链、request ID、结构化日志、构造器注入并实现优雅停机。多表写入用事务，状态迁移用条件更新，唯一性/幂等不能只靠前端。禁止 TODO、placeholder、空函数、硬编码成功响应和 handler 直连数据库。
- Go 功能代码目标 2500-4200 行、24-42 个功能 `.go` 文件且少于 5000 行；不计 `_test.go`、生成代码和 vendor。
- 只保留普通项目单测；不得创建名称含 record/regression/grading/scoring 的评分测试，不得加入答案、`BUG_REPRO.md` 或标注流水线内部文件。
- 根 `go.work` 纳入 `./backend`，从根目录运行 `go work sync`、`go build ./backend/...`、`go test ./backend/...` 成功。

```text
.
├── go.work
├── runtime_smoke.json
├── docker-compose.yml
├── .env
├── .env.example
├── .gitignore
├── README.md
├── output/
│   └── execution.md
├── backend/
│   ├── go.mod  Dockerfile
│   ├── cmd/server/main.go
│   └── internal/
│       ├── config/ constants/ dto/ model/
│       ├── repository/ service/ handler/ router/
│       ├── middleware/ algorithm/ geometry/ util/
├── frontend/
│   ├── package.json  Dockerfile  nginx.conf
│   └── src/
│       ├── api/ stores/ types/ components/common/
│       ├── hooks/ pages/ router/ utils/
└── database/init.sql
```

四实体必须在每个后端层和前端 api/store/type 中各自分文件；严禁合并职责到单一文件，也不得以一个通用 CRUD handler 代替实体模块。

## 8. 运行 Manifest、部署与验证交付

- 固定端口：前端 `18532`、后端 `19532`、PostgreSQL 宿主机 `57532`；容器内分别为 `80`、`8080`、`5432`。
- 根 `runtime_smoke.json` 的唯一用途是提供可解析的启动 manifest，内容必须严格等价于：

```json
{
  "mode": "service",
  "start": "go run ./cmd/server",
  "workdir": "backend",
  "env": {
    "PORT": "20532",
    "DB_DRIVER": "sqlite",
    "DB_DSN": "file:runtime-smoke?mode=memory&cache=shared",
    "JWT_SECRET": "runtime-smoke-only-change-me"
  },
  "ready_url": "http://127.0.0.1:20532/healthz",
  "ready_status": [200],
  "startup_timeout": 30
}
```

- 后端必须支持上述 SQLite smoke 配置并自动建表/种子数据；正式 Compose 使用 PostgreSQL/PostGIS。manifest 禁止出现 `passed`、截图或证据字段。
- Compose 顶层 `name: sonar-survey-coverage-planner`，不得写 `version:`；包含 `db/backend/frontend`、PostGIS 镜像和命名卷，所有容器名使用英文短名前缀，数据库、后端和前端均有 healthcheck，并按健康条件依赖。
- `.env`、`.env.example` 均包含 `COMPOSE_PROJECT_NAME=sonar-survey-coverage-planner`、`FRONTEND_PORT=18532`、`BACKEND_PORT=19532`、`DB_PORT=57532`、`DB_DRIVER=postgres`、DB 变量和 `JWT_SECRET`；`.env` 不提交。前端只调用 `/api`，Nginx 原样代理 `/api/v1` 到 `backend:8080`，另提供 `/api/healthz` 并处理 SPA 路由。
- README 首屏提供 `docker compose up -d`，说明项目介绍、主要功能、角色账号、架构与目录、API 清单、技术栈表、环境变量与端口、本地开发、Docker 部署、枚举位置、坐标与算法边界、runtime smoke、构建测试、常见问题、停止命令和 License；README 不得承载执行证据。
- 必须真实运行 `go work sync`、`go build ./backend/...`、`go vet ./backend/...`、`go test ./backend/...`、`npm --prefix frontend ci`、`npm --prefix frontend run build`、`python3 /Users/gaobo/.codex/skills/go-annotation-pipeline/scripts/project_scale.py .`、`python3 /Users/gaobo/.codex/skills/go-annotation-pipeline/scripts/runtime_smoke.py .`、`docker compose config --quiet`、`docker compose up -d --build`、`docker compose ps`；前后端均须有 healthcheck，所有长驻服务必须 healthy。
- 至少完成 8 个真实接口冒烟：`/healthz`、登录、四实体列表、测区创建、航迹导入、合法/非法状态迁移、覆盖计算与结果读取、无权限 403；至少包含 8 条且覆盖成功和失败分支。
- API 冒烟还必须包含详情读取、未认证 401 与越权 403；四个核心实体各至少经过一条真实后端链路，五个页面都消费真实 `/api/v1`，不得使用静态 mock。并在 `backend/` 内复跑 `go build ./...`、`go vet ./...`、`go test ./...`。
- 只用 Codex 内置 Browser 验证登录、测区、规划、航迹、覆盖五页交互，禁止外部 Chrome；实际完成创建或编辑、列表刷新和一次状态变化，并检查 console/network、文字溢出和遮挡无阻断异常。
- 所有构建、测试、runtime smoke、Compose、至少 8 条 API、Browser、console/network 和关闭证据只能写入 `output/execution.md`；截图必须直接放 `output/`，至少桌面与移动端各一张并由 execution.md 引用。禁止在其他文件复制证据或伪造结果。
- `output/execution.md` 必须包含项目编号、验证时间、实际端口、规模实测、根与 backend 构建/vet/test、前端构建、服务 healthy、API 表、Browser 操作步骤、截图相对路径、逐条需求覆盖、修复摘要、停服无残留和实现提交 hash。
- 验证后执行 `docker compose down -v --remove-orphans`，确认容器、网络和命名卷均已清理，同时停止 runtime smoke 进程。
- 项目必须独立 `git init`，设置本地身份 `git config user.name "blueship581"`、`git config user.email "brysj.hhrhl.g@gmail.com"`。真实实现与验证完成后提交 `git add -A && git commit -m "feat: implement and verify sonar-survey-coverage-planner"`，把实现提交 hash 写入 `output/execution.md`，再执行 `git add output/execution.md && git commit -m "docs: record verified execution"`，形成两次提交。`.gitignore` 排除 `.env`、依赖、构建产物、日志和本地 SQLite 文件，但保留 execution.md 与截图；最终 `git status --short` 为空，不得共用 Git 仓库。
